from setuptools import setup, find_packages # pyright: ignore[reportMissingModuleSource]

setup(
    name="reachy_ultradancemix_9000",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add your actual dependencies here
        # Example: "torch>=2.0.0", "torchaudio>=2.0.0", etc.
    ],
)
